# Productive utilization rate calculator

A small static web page for estimating **annual utilization** from twelve monthly percentages, then adjusting that figure for **PTO** against a **2,000 hour** working-year baseline (**8 hours** per PTO day). Everything runs in the browser; there is no backend.